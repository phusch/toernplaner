diff -ru /mnt/data/tp_orig/Toernplaner_V1.4.2_overview_gesamtbetrag/index.html /mnt/data/Toernplaner_V1.4.2_storage_status/index.html
--- /mnt/data/tp_orig/Toernplaner_V1.4.2_overview_gesamtbetrag/index.html	2026-06-08 14:17:24.000000000 +0000
+++ /mnt/data/Toernplaner_V1.4.2_storage_status/index.html	2026-06-08 15:45:52.374860771 +0000
@@ -386,7 +386,7 @@
 
 function syncInputs(){ensureGiftmannParticipation(data);['giftMode','surplusReduces','dirkBoardCash'].forEach(id=>{const el=document.getElementById(id);if(el)el.value=data[id]});['giftAmount','depositPerBoat','boats'].forEach(id=>{const el=document.getElementById(id);if(el)el.value=data[id]})}
 function readInputs(){const giftModeEl=document.getElementById('giftMode'),dirkEl=document.getElementById('dirkBoardCash');data.giftMode=giftModeEl?giftModeEl.value:(data.giftMode||'Nur-Schenker');data.surplusReduces=val('surplusReduces');data.dirkBoardCash=dirkEl?dirkEl.value:(data.dirkBoardCash||'Ja');data.giftAmount=+val('giftAmount')||0;data.depositPerBoat=+val('depositPerBoat')||0;data.boats=+val('boats')||0;ensureGiftmannParticipation(data);(data.costs||[]).forEach(c=>{c.area=normalizeCostArea(c.area);if(!c.calcType)c.calcType=c.area==='Mannschaftskasse'?'period-person':'pauschal';if(c.factor===undefined||c.factor===null||c.factor==='')c.factor=1})}
-function save(silent=true){try{readInputs();const t=getActiveTrip();if(t){t.data=clone(data);t.updatedAt=nowIso()}persistState();saveGuardSetClean();if(!silent)toast('Gespeichert – '+(t?t.name:'Törn'))}catch(e){if(!silent)toast('Speichern im Browser nicht möglich')}}
+function save(silent=true){try{readInputs();const t=getActiveTrip();if(t){t.data=clone(data);t.updatedAt=nowIso()}persistState();saveGuardSetClean();if(!silent)toast('Lokal gespeichert: '+niceDate(t?t.updatedAt:nowIso()))}catch(e){if(!silent)toast('Speichern im Browser nicht möglich')}}
 function updatePageNote(page,value){const t=getActiveTrip();if(!t)return;t.pageNotes=t.pageNotes||{};t.pageNotes[page]=value;t.updatedAt=nowIso();persistState();updateStatus()}
 function renderPageNotes(){const t=getActiveTrip();const notes=(t&&t.pageNotes)||{};['trips','overview','people','settings','admin','results'].forEach(k=>{const el=document.getElementById('note-'+k);if(el&&el.value!==(notes[k]||''))el.value=notes[k]||''})}
 function renderTripHome(){const box=document.getElementById('tripList');if(!box)return;box.innerHTML='';appState.trips.forEach(migrateTrip);appState.trips.slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt))).forEach(t=>{const c=quickCalc(t.data);const days=tripDays(t);const div=document.createElement('div');div.className='trip-card';div.innerHTML=`<h3>${escapeHtml(t.name)}</h3><div class="trip-name-row"><label class="small">Törnname</label><input class="trip-name-edit" type="text" value="${escapeHtml(t.name||'')}" placeholder="Törnname vergeben" onchange="updateTripName('${t.id}',this.value)"></div><div class="trip-meta">Zeitraum: ${escapeHtml(tripPeriod(t))} · ${days} Tag${days===1?'':'e'}<br>Lokal gespeichert: ${niceDate(t.updatedAt)}<br>Cloud: ${t.cloudMeta?('gespeichert '+niceDate(t.cloudMeta.savedAt||t.cloudMeta.updatedAt)+' · Rev. '+(t.cloudMeta.revision||'–')):'noch nicht gespeichert'}<br>Mitreisende: ${c.payingTravelers} · 1. Überweisung: ${eur(c.transferPer)}</div><label class="small">Zeitraum von</label><input type="date" value="${escapeHtml(t.startDate||'')}" onchange="updateTripField('${t.id}','startDate',this.value)"><label class="small">Zeitraum bis</label><input type="date" value="${escapeHtml(t.endDate||'')}" onchange="updateTripField('${t.id}','endDate',this.value)"><label class="small">Bemerkung</label><textarea rows="3" placeholder="Bemerkungen zum Törn, Zeitraum, Treffpunkt ..." onchange="updateTripField('${t.id}','notes',this.value)">${escapeHtml(t.notes||'')}</textarea><div class="trip-actions"><button class="primary" onclick="openTrip('${t.id}')">${t.id===appState.activeTripId?'Geöffnet':'Öffnen'}</button><button class="secondary" onclick="renameTrip('${t.id}')">Name per Dialog</button><button class="secondary" onclick="duplicateTrip('${t.id}')">Duplizieren</button><button class="danger" onclick="deleteTrip('${t.id}')">Löschen</button></div>`;box.appendChild(div)})}
@@ -712,7 +712,7 @@
 }
 
 function update(){readInputs();const t=getActiveTrip();if(t)t.data=clone(data);const c=calc(false);renderPeople(c);renderCostOverview(c);renderCosts(c);renderResults(c);renderParticipantManagement(c);renderTripHome();renderPageNotes();updateStatus()}
-function updateStatus(){const t=getActiveTrip();const saved=t?niceDate(t.updatedAt):'nicht gespeichert';const name=t?escapeHtml(t.name):'Kein Törn';const dirty=hasUnsavedChanges();const guard=dirty?'<span class="save-dirty">Ungespeichert</span>':'<span class="save-clean">Gespeichert</span>';document.getElementById('activeTripBadge').innerHTML=`Aktiver Törn: <b>${name}</b> · Zeitraum: ${escapeHtml(tripPeriod(t))} · letzter gespeicherter Stand: ${saved} · ${guard}`;updateSaveGuardStatus();const sd=document.getElementById('standDate');if(sd)sd.textContent='Stand: '+saved+(dirty?' · Ungespeichert':'');document.querySelectorAll('.status').forEach(el=>{if(el.id!=='standDate'&&el.id!=='cloudStatus')el.textContent='Letzte Speicherung: '+saved+(dirty?' · Ungespeichert':'')})}
+function updateStatus(){const t=getActiveTrip();const saved=t?niceDate(t.updatedAt):'nicht gespeichert';const name=t?escapeHtml(t.name):'Kein Törn';const dirty=hasUnsavedChanges();const guard=dirty?'<span class="save-dirty">Ungespeichert</span>':'<span class="save-clean">Gespeichert</span>';document.getElementById('activeTripBadge').innerHTML=`Aktiver Törn: <b>${name}</b> · Zeitraum: ${escapeHtml(tripPeriod(t))} · lokal gespeichert: ${saved} · ${guard}`;updateSaveGuardStatus();const sd=document.getElementById('standDate');if(sd)sd.textContent='Stand lokal gespeichert: '+saved+(dirty?' · Ungespeichert':'');document.querySelectorAll('.status').forEach(el=>{if(el.id!=='standDate'&&el.id!=='cloudStatus')el.textContent='Lokal gespeichert: '+saved+(dirty?' · Ungespeichert':'')})}
 
 
 // -----------------------------------------------------------------------------
@@ -747,9 +747,9 @@
   t.cloudMeta={cloudTripId:cloudTrip.id,revision:stateRow?.revision||null,savedAt:stateRow?.updated_at||nowIso(),appVersion:CLOUD_APP_VERSION};
   return stateRow;
 }
-async function cloudSaveActiveTrip(){try{const user=await cloudCurrentUser();await cloudUpsertProfile(user);manualSave();const t=getActiveTrip();const supa=await cloudEnsureClient();const stateRow=await cloudSaveTripToSupabase(t,user,supa);persistState();saveGuardSetClean();renderTripHome();cloudSetStatus('Cloud gespeichert: '+(t.name||'Törn')+' · Revision '+(stateRow?.revision||'–'));toast('Aktiver Törn in Cloud gespeichert')}catch(e){cloudSetStatus(e.message||String(e),true);toast('Cloud-Speichern fehlgeschlagen')}}
-async function cloudSaveAllTrips(){try{const user=await cloudCurrentUser();await cloudUpsertProfile(user);manualSave();const supa=await cloudEnsureClient();let count=0,lastRevision='–';for(const t of appState.trips){migrateTrip(t);const row=await cloudSaveTripToSupabase(t,user,supa);count++;lastRevision=row?.revision||lastRevision}persistState();saveGuardSetClean();renderTripHome();cloudSetStatus(count+' lokale Törn(e) in Cloud gespeichert. Letzte Revision '+lastRevision);toast('Alle Törns in Cloud gespeichert')}catch(e){cloudSetStatus(e.message||String(e),true);toast('Alle Törns speichern fehlgeschlagen')}}
-async function cloudLoadTripList(){try{await cloudCurrentUser();const supa=await cloudEnsureClient();const {data:rows,error}=await supa.from('trips').select('id,legacy_local_id,name,client_updated_at,updated_at').is('deleted_at',null).order('updated_at',{ascending:false});if(error)throw error;cloudTripsCache=rows||[];const sel=document.getElementById('cloudTripSelect');if(sel){sel.innerHTML='';if(!cloudTripsCache.length){sel.innerHTML='<option value="">Keine Cloud-Törns gefunden</option>'}else{cloudTripsCache.forEach(t=>{const opt=document.createElement('option');opt.value=t.id;opt.textContent=(t.name||'Törn')+' · '+niceDate(t.client_updated_at||t.updated_at);sel.appendChild(opt)})}}cloudSetStatus(cloudTripsCache.length+' Cloud-Törn(e) geladen. Zum Übernehmen auswählen und Button klicken.');toast('Cloud-Liste geladen')}catch(e){cloudSetStatus(e.message||String(e),true);toast('Cloud-Liste konnte nicht geladen werden')}}
+async function cloudSaveActiveTrip(){try{const user=await cloudCurrentUser();await cloudUpsertProfile(user);manualSave();const t=getActiveTrip();const supa=await cloudEnsureClient();const stateRow=await cloudSaveTripToSupabase(t,user,supa);const cloudSavedAt=stateRow?.updated_at||stateRow?.client_updated_at||t.cloudMeta?.savedAt||nowIso();persistState();saveGuardSetClean();renderTripHome();cloudSetStatus('Cloud gespeichert: '+niceDate(cloudSavedAt)+' · Revision '+(stateRow?.revision||'–'));toast('Cloud gespeichert: '+niceDate(cloudSavedAt)+' · Revision '+(stateRow?.revision||'–'))}catch(e){cloudSetStatus(e.message||String(e),true);toast('Cloud-Speichern fehlgeschlagen')}}
+async function cloudSaveAllTrips(){try{const user=await cloudCurrentUser();await cloudUpsertProfile(user);manualSave();const supa=await cloudEnsureClient();let count=0,lastRevision='–',lastSavedAt=nowIso();for(const t of appState.trips){migrateTrip(t);const row=await cloudSaveTripToSupabase(t,user,supa);count++;lastRevision=row?.revision||lastRevision;lastSavedAt=row?.updated_at||row?.client_updated_at||lastSavedAt}persistState();saveGuardSetClean();renderTripHome();cloudSetStatus('Cloud gespeichert: '+niceDate(lastSavedAt)+' · '+count+' Törn(e) · letzte Revision '+lastRevision);toast('Cloud gespeichert: '+niceDate(lastSavedAt)+' · '+count+' Törn(e)')}catch(e){cloudSetStatus(e.message||String(e),true);toast('Alle Törns speichern fehlgeschlagen')}}
+async function cloudLoadTripList(){try{await cloudCurrentUser();const supa=await cloudEnsureClient();const {data:rows,error}=await supa.from('trips').select('id,legacy_local_id,name,client_updated_at,updated_at').is('deleted_at',null).order('updated_at',{ascending:false});if(error)throw error;cloudTripsCache=rows||[];const sel=document.getElementById('cloudTripSelect');if(sel){sel.innerHTML='';if(!cloudTripsCache.length){sel.innerHTML='<option value="">Keine Cloud-Törns gefunden</option>'}else{cloudTripsCache.forEach(t=>{const opt=document.createElement('option');opt.value=t.id;opt.textContent=niceDate(t.client_updated_at||t.updated_at)+' · '+(t.name||'Törn');sel.appendChild(opt)})}}cloudSetStatus(cloudTripsCache.length+' Cloud-Törn(e) geladen. Zum Übernehmen auswählen und Button klicken.');toast('Cloud-Liste geladen')}catch(e){cloudSetStatus(e.message||String(e),true);toast('Cloud-Liste konnte nicht geladen werden')}}
 async function cloudImportSelectedTrip(asCopy=false){
   try{
     if(!saveGuardConfirm('Einen Cloud-Törn laden'))return;
diff -ru /mnt/data/tp_orig/Toernplaner_V1.4.2_overview_gesamtbetrag/sw.js /mnt/data/Toernplaner_V1.4.2_storage_status/sw.js
--- /mnt/data/tp_orig/Toernplaner_V1.4.2_overview_gesamtbetrag/sw.js	2026-06-08 14:17:24.000000000 +0000
+++ /mnt/data/Toernplaner_V1.4.2_storage_status/sw.js	2026-06-08 15:45:52.375171996 +0000
@@ -1,4 +1,4 @@
-const CACHE_NAME = 'chillout-pirates-kosten-v142-overview-total-v1';
+const CACHE_NAME = 'chillout-pirates-kosten-v142-storage-status-v1';
 const ASSETS = [
   './',
   './index.html',
@@ -24,4 +24,4 @@
   }).catch(() => caches.match('./index.html'))));
 });
 
-// v1.4.2 overview total display 2026-06-08
+// v1.4.2 storage status display 2026-06-08
