--- /mnt/data/Toernplaner_V1.5.2_overview_installment_summary/index.html	2026-07-02 08:31:54.030851360 +0000
+++ /mnt/data/Toernplaner_V1.5.3_installments_per_traveler/index.html	2026-07-02 08:33:11.486535237 +0000
@@ -210,9 +210,9 @@
       <div class="kpis">
         <div class="kpi"><div class="emoji">👥</div><span>Mitreisende zahlen</span><b id="kpiTrip">–</b><span>pro Person ohne Kaution</span></div>
         <div class="kpi"><div class="emoji">🪙</div><span>Mitreisende inkl. Kaution</span><b id="kpiTransfer">–</b><span>pro Mitreisendem</span></div>
-        <div class="kpi"><div class="emoji">1️⃣</div><span>1. Anzahlung</span><b id="kpiRate1">–</b><span>angefordert gesamt</span></div>
-        <div class="kpi"><div class="emoji">2️⃣</div><span>2. Anzahlung</span><b id="kpiRate2">–</b><span>angefordert gesamt</span></div>
-        <div class="kpi"><div class="emoji">3️⃣</div><span>3. Anzahlung</span><b id="kpiRate3">–</b><span>angefordert gesamt</span></div>
+        <div class="kpi"><div class="emoji">1️⃣</div><span>1. Anzahlung</span><b id="kpiRate1">–</b><span>pro Mitreisendem</span></div>
+        <div class="kpi"><div class="emoji">2️⃣</div><span>2. Anzahlung</span><b id="kpiRate2">–</b><span>pro Mitreisendem</span></div>
+        <div class="kpi"><div class="emoji">3️⃣</div><span>3. Anzahlung</span><b id="kpiRate3">–</b><span>pro Mitreisendem</span></div>
         <div class="kpi gift"><div class="emoji">🎁</div><span>Schenker zahlen</span><b id="kpiGift">–</b><span>pro Person</span></div>
         <div class="kpi"><div class="emoji">🧍</div><span>Giftmann zahlt selbst</span><b id="kpiGiftman">–</b><span>nach Geschenklogik ohne Kaution</span></div>
       </div>
@@ -365,7 +365,7 @@
 v1.3 Törnnamen: direkt editierbar, Cloud-Kopie mit Wunschname
 v1.4.2 Giftmann-Logik: Häkchen bedeuten geschenkte Bereiche; nicht geschenkte Bereiche zahlt der Giftmann selbst; Rest nur auf Mitreisende
 v1.5 Anzahlungsraten: Fixkosten als 1. Rate, Mannschaftskasse + Sonstige + Kaution als 2. Rate, frei definierbare 3. Rate; alle Anforderungsbeträge frei überschreibbar
-v1.5.2 Übersicht: Summen der drei Anzahlungen, Rundung auf zwei Nachkommastellen und Rollensortierung bei Einzelbeträgen</div>
+v1.5.2 Übersicht: Summen der drei Anzahlungen, Rundung auf zwei Nachkommastellen und Rollensortierung bei Einzelbeträgen<br>v1.5.3 Übersicht: Anzahlungen pro Mitreisendem statt Gesamtsummen</div>
         </section>
       </div>
     </div>
@@ -896,32 +896,46 @@
     body.appendChild(tr);
   });
 }
-function calculateInstallmentTotals(c){
+function calculateInstallmentsPerTraveler(c){
   ensureParticipantInfo();
   const rowByName={};
   (c.rows||[]).forEach(r=>rowByName[r.name]=r);
-  const totals=[0,0,0];
-  (data.people||[]).filter(p=>p&&p[0]).forEach(p=>{
+  const values=[[],[],[]];
+  (data.people||[]).filter(p=>p&&p[0]&&normalizeRole(p[1])==='Vollzahler').forEach(p=>{
     const name=p[0],role=normalizeRole(p[1]);
     const calcRow=rowByName[name]||{fixedDue:0,crewFundDue:0,otherDue:0,deposit:0,giftDue:0};
     const info=data.participantInfo[participantKey(name)]||{};
-    const rates=role==='Schenker'?[1]:[1,2,3];
-    rates.forEach(number=>{
+    [1,2,3].forEach(number=>{
       const suggested=installmentSuggestion(calcRow,role,number);
-      totals[number-1]+=installmentRequested(info,'rate'+number,suggested);
+      values[number-1].push(installmentRequested(info,'rate'+number,suggested));
     });
   });
-  return totals.map(money2);
+  return values.map(list=>{
+    if(!list.length)return{value:0,different:false,min:0,max:0};
+    const rounded=list.map(money2);
+    const min=Math.min(...rounded),max=Math.max(...rounded);
+    return{value:rounded[0],different:Math.abs(max-min)>=0.005,min,max};
+  });
+}
+function showInstallmentPerTraveler(elementId,entry){
+  const el=document.getElementById(elementId);if(!el)return;
+  if(entry.different){
+    el.textContent='individuell';
+    el.title=`Unterschiedliche angeforderte Beträge: ${eur(entry.min)} bis ${eur(entry.max)}`;
+  }else{
+    el.textContent=eur(entry.value);
+    el.removeAttribute('title');
+  }
 }
 function renderResults(c){
   document.getElementById('kpiTrip').textContent=eur(c.tripPer);
   document.getElementById('kpiTransfer').textContent=eur(c.transferPer);
   document.getElementById('kpiGift').textContent=eur(data.giftAmount);
   document.getElementById('kpiGiftman').textContent=eur(c.giftmannPaysSelf);
-  const installmentTotals=calculateInstallmentTotals(c);
-  document.getElementById('kpiRate1').textContent=eur(installmentTotals[0]);
-  document.getElementById('kpiRate2').textContent=eur(installmentTotals[1]);
-  document.getElementById('kpiRate3').textContent=eur(installmentTotals[2]);
+  const installmentsPerTraveler=calculateInstallmentsPerTraveler(c);
+  showInstallmentPerTraveler('kpiRate1',installmentsPerTraveler[0]);
+  showInstallmentPerTraveler('kpiRate2',installmentsPerTraveler[1]);
+  showInstallmentPerTraveler('kpiRate3',installmentsPerTraveler[2]);
   document.getElementById('countMit').textContent=c.payingTravelers;
   document.getElementById('countAboard').textContent=c.aboard;
   document.getElementById('countGift').textContent=c.onlyGift;
--- /mnt/data/Toernplaner_V1.5.2_overview_installment_summary/sw.js	2026-07-02 08:13:21.515879546 +0000
+++ /mnt/data/Toernplaner_V1.5.3_installments_per_traveler/sw.js	2026-07-02 08:33:11.486849686 +0000
@@ -1,4 +1,4 @@
-const CACHE_NAME = 'chillout-pirates-kosten-v152-overview-installments-v1';
+const CACHE_NAME = 'chillout-pirates-kosten-v153-installments-per-traveler-v1';
 const ASSETS = [
   './',
   './index.html',
--- /mnt/data/Toernplaner_V1.5.2_overview_installment_summary/README.md	2026-07-02 08:31:53.872101443 +0000
+++ /mnt/data/Toernplaner_V1.5.3_installments_per_traveler/README.md	2026-07-02 08:33:11.487240953 +0000
@@ -1,3 +1,15 @@
+# Törnplaner – V1.5.3 Anzahlungen pro Mitreisendem
+
+## Änderung gegenüber V1.5.2
+
+- Die drei Anzahlungs-Kästen auf der Übersichtsseite zeigen jetzt den angeforderten Betrag **pro Mitreisendem** statt der Gesamtsumme aller Teilnehmer.
+- Frei eingegebene Rundungsbeträge aus der Teilnehmerverwaltung werden berücksichtigt.
+- Sind bei mehreren Mitreisenden unterschiedliche Anforderungsbeträge eingetragen, zeigt der betreffende Kasten „individuell“; ein Hinweis nennt beim Berühren/Überfahren die Spanne.
+- Schenkerbeträge werden nicht mehr in den Anzahlungs-Kästen berücksichtigt.
+- Kostenberechnung, Rollenlogik, Giftmann-Logik und Supabase wurden nicht verändert.
+
+---
+
 # Törnplaner / Chillout Pirates Kalkulation – V1.4.4 Dynamischer Törntitel
 
 ## Visuelle Weiterentwicklung V1.4.4 – aktiver Törn in der Kopfzeile
